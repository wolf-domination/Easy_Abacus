// react-vite/src/components/BinaryRows/BinaryRows.jsx
// No-op version to remove the Binary (1/0) grid everywhere it's used.
// Keeps the same import/usage surface so nothing else breaks.

export default function BinaryRows() {
  return null; // render nothing
}
